# Example: Minimal configuration with auth disabled
branding:
  logo_url: "/minimal-logo.svg"
  title: "Demo Agent"

features:
  debug_mode_default: false
  memory_enabled_default: false
  auth_enabled: false  # No login required

agent:
  endpoint: "https://demo-backend.example.com"
  timeout_ms: 60000
  streaming: true
