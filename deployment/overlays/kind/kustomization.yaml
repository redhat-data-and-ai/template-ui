apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: template-ui

resources:
  - ../../base
  - ingress.yaml

labels:
  - pairs:
      app: template-ui
      component: ui

images:
  - name: template-ui
    newName: localhost/template-ui
    newTag: local
